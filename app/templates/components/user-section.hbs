{{#if user.picture}}
  <img src={{user.picture}} alt="Profile Picture" class="profile-picture" />
{{/if}}
<h1>
  {{user.name}}
</h1>
{{#if user.signOut}}
  <button {{action signOut}}>Sign Out</button>
{{/if}}
